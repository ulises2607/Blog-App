<h1> Vista para crear un nuevo comentario </h1>

<%= form_for([@user, @post, @comment]) do |f| %>
  <div class="field">
    <%= f.label :text %>
    <%= f.text_area :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', user_post_path(@user, @post) %>